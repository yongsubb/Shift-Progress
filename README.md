# Shift-Progress

A Flutter-based shift tracking app with schedule management, salary records,
and PWA support.

## Getting Started

1. Install Flutter SDK.
2. Run `flutter pub get`.
3. Run `flutter run`.
